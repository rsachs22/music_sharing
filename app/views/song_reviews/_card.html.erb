<div class="card mb-3">
  <h2 class="card-header h4">
    Song Review #<%= song_review.id %>
  </h2>

  <div class="card-body">
    <dl class="mb-1">
      <dt>User</dt>
      <!-- Display user_id start -->
      <dd><%= song_review.user_id %></dd>
      <!-- Display user_id end -->

      <dt>Body</dt>
      <!-- Display body start -->
      <dd><%= song_review.body %></dd>
      <!-- Display body end -->

      <dt>Title</dt>
      <!-- Display title start -->
      <dd><%= song_review.title %></dd>
      <!-- Display title end -->

      <dt>Stars</dt>
      <!-- Display stars start -->
      <dd><%= song_review.stars %></dd>
      <!-- Display stars end -->

      <dt>Song</dt>
      <!-- Display song_id start -->
      <dd><%= song_review.song_id %></dd>
      <!-- Display song_id end -->

    </dl>

    <div class="row">
      <div class="col">
        <%= link_to_show_or_back song_review, '<i class="fas fa-search-plus"></i>'.html_safe, '<i class="fas fa-chevron-left"></i>'.html_safe, class: "btn btn-block btn-outline-secondary" %>
      </div>

      <!-- Edit link song_review start -->
      <div class="col">
        <%= link_to edit_song_review_url(song_review), class: "btn btn-block btn-outline-secondary" do %>
          <i class="fas fa-edit"></i>
        <% end %>
      </div>
      <!-- Edit link song_review end -->

      <!-- Delete link song_review start -->
      <div class="col">
        <%= link_to song_review, method: :delete, data: { confirm: "Do you really want to delete this song_review?" }, class: "btn btn-block btn-outline-secondary" do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>
      </div>
      <!-- Delete link song_review end -->
    </div>
  </div>

  <div class="card-footer text-muted">
    Last updated <%= time_ago_in_words(song_review.updated_at) %> ago
  </div>
</div>
